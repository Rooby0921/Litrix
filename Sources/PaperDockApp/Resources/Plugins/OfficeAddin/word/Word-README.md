# Word 安装说明

1. 运行上一级目录中的 `start-local-server.command`。
2. 在 Word 中打开加载项侧载入口。
3. 选择本目录的 `LitrixWordManifest.xml`。
4. 功能区会出现 `Litrix` 标签页，包含 `引文格式` 和 `引用格式` 两个入口。

默认任务面板地址：

```text
http://127.0.0.1:23121/common/taskpane.html
```
